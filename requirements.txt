# First capstone environment: vLLM rollout in the active `RL/LLM` repo.
# Recommended workflow:
#   conda create -n llm-vllm-rollout python=3.12 -y
#   conda activate llm-vllm-rollout
#   pip install -r requirements.txt
#
# Notes:
# - This file is intentionally curated from the working `rl-eng` env instead of
#   using a full `pip freeze` dump.
# - Keep separate conda envs later for `deepspeed`, `sglang`, and `nemo`.
# - `LLM-baseline` stays on its own `uv` workflow.

accelerate==1.13.0
datasets==4.7.0
modal==1.3.5
numpy==2.2.6
peft==0.18.1
sentencepiece==0.2.1
torch==2.10.0+cu128
tqdm==4.67.3
transformers==4.57.6
vllm==0.17.0
wandb==0.25.0

# Optional once the core vLLM rollout env is stable:
# flash_attn==2.8.3